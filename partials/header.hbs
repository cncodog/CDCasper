<header class="main-header"  style="background-image: url({{@blog.cover}})">

    <div class="container">
        <div class="row">
            <div class="col-sm-12">

                <!-- start logo -->
                <a class="branding" href="{{@blog.url}}" title="{{@blog.title}}"><img src="{{@blog.logo}}" alt="{{@blog.title}}"></a>
                <!-- end logo -->
                <h2 class="text-hide">{{@blog.description}}</h2>

                <img src="{{@blog.logo}}" alt="{{@blog.title}}" class="hide">
            </div>
        </div>
    </div>
</header>
